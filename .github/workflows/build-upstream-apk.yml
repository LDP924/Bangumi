name: Build upstream signed APK

on:
  workflow_dispatch:
    inputs:
      release_notes:
        description: '发行版更新日志 (上游提交记录, sync 工作流自动传入; 留空则自动从 git log 生成)'
        required: false
        type: string

permissions:
  contents: write

concurrency:
  group: build-upstream-apk
  cancel-in-progress: false

jobs:
  build:
    name: Build signed APK
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          # 拉全量历史, 用于自动生成发行版日志 (git log 上游提交)
          fetch-depth: 0

      - name: Set up Node (LTS 固定, 拒绝漂移)
        uses: actions/setup-node@v6
        with:
          # RN 0.86 / Expo 57 要求 node >= 20; 固定 22 LTS
          # (不用 latest: 每逢偶数版大版本发布构建链就随机炸)
          node-version: '22'
          check-latest: true

      - name: Resolve build version
        id: version
        run: |
          set -euo pipefail
          VERSION=$(node -pe "require('./app.json').expo.version")
          SHORT_SHA="${GITHUB_SHA:0:7}"
          BUILD_TAG="apk-v${VERSION}-${SHORT_SHA}"
          ASSET_NAME="bangumi_v${VERSION}_${SHORT_SHA}_arm64-v8a.apk"
          CLONE_ASSET_NAME="bangumi_clone_v${VERSION}_${SHORT_SHA}_arm64-v8a.apk"
          echo "build_tag=$BUILD_TAG" >> "$GITHUB_OUTPUT"
          echo "asset_name=$ASSET_NAME" >> "$GITHUB_OUTPUT"
          echo "clone_asset_name=$CLONE_ASSET_NAME" >> "$GITHUB_OUTPUT"
          echo "Building $BUILD_TAG (source: $GITHUB_SHA)"

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'
          check-latest: true
          cache: gradle

      - name: Accept SDK licenses (写文件绕交互, 见 run 130/131 教训)
        run: |
          # runner 升级 cmdline-tools 20.0 后: setup-android 的 --licenses 交互挂死,
          # yes|sdkmanager 也 Broken pipe 静默拒装 NDK —— 直接写 license hash 文件
          export ANDROID_HOME="${ANDROID_HOME:-/usr/local/lib/android/sdk}"
          mkdir -p "$ANDROID_HOME/licenses"
          printf '\n8933bad161af4178b1185d48a372f1c35230d97b\n24333f8a63b6825ea9c5514f83c2829b004d1fee\n' \
            > "$ANDROID_HOME/licenses/android-sdk-license"
          printf '\nd975f751698a77b662f1254ddbeed3901e976f5a\n' \
            > "$ANDROID_HOME/licenses/android-sdk-preview-license" || true
          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$PATH"
          sdkmanager --version

      - name: Install NDK versions (装完必验证, 静默失败直接炸)
        run: |
          export ANDROID_HOME="${ANDROID_HOME:-/usr/local/lib/android/sdk}"
          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$PATH"
          # 23.1: android/build.gradle ndkVersion (RN 层)
          # 27.1: android/rust 原生库构建
          sdkmanager "ndk;23.1.7779620" "ndk;27.1.12297006" > /dev/null
          for v in 23.1.7779620 27.1.12297006; do
            if [ ! -d "$ANDROID_HOME/ndk/$v" ]; then
              echo "::error::NDK $v 未装上 (sdkmanager 静默失败)"
              exit 1
            fi
          done
          echo "NDK 23.1 + 27.1 就绪"

      - name: Cache Yarn global cache
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/yarn
            ~/.cache/node
          key: yarn-global-${{ runner.os }}
          restore-keys: |
            yarn-global-${{ runner.os }}

      - name: Decode Android keystore
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          set -euo pipefail

          KEYSTORE_PATH="$GITHUB_WORKSPACE/Bangumi.jks"
          echo "$ANDROID_KEYSTORE_BASE64" | base64 --decode > "$KEYSTORE_PATH"
          echo "KEYSTORE_PATH=$KEYSTORE_PATH" >> "$GITHUB_ENV"

          if [ ! -s "$KEYSTORE_PATH" ]; then
            echo "::error::Keystore file is empty (0 bytes) at $KEYSTORE_PATH"
            echo "::error::Verify ANDROID_KEYSTORE_BASE64 secret exists in Settings > Secrets and variables > Actions."
            exit 1
          fi

          echo "Keystore size: $(stat -c%s "$KEYSTORE_PATH") bytes"

      - name: Prepare APK source
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"

          node packages/env.js android

          if [ -f android/rust/build-android.sh ]; then
            sed 's/darwin-x86_64/linux-x86_64/g' android/rust/build-android.sh > android/rust/build-android-linux.sh
            chmod +x android/rust/build-android-linux.sh
          fi

          # 上游用腾讯镜像的 gradle 分发包, CI 上换回官方源 (国内镜像海外 runner 反而慢/不稳)
          sed -i 's|https\\://mirrors.cloud.tencent.com/gradle/|https\\://services.gradle.org/distributions/|g' android/gradle/wrapper/gradle-wrapper.properties || true

          cp android/gradle_yours.properties android/gradle.properties

          sed -i '/^org\.gradle\.configureondemand=/d' android/gradle.properties
          sed -i '/^org\.gradle\.configuration-cache=/d' android/gradle.properties
          sed -i '/^org\.gradle\.daemon=/d' android/gradle.properties
          echo 'org.gradle.configureondemand=false' >> android/gradle.properties

          sed -i '/^MYAPP_RELEASE_STORE_FILE=/d' android/gradle.properties
          sed -i '/^MYAPP_RELEASE_KEY_ALIAS=/d' android/gradle.properties
          sed -i '/^MYAPP_RELEASE_STORE_PASSWORD=/d' android/gradle.properties
          sed -i '/^MYAPP_RELEASE_KEY_PASSWORD=/d' android/gradle.properties

          node <<'NODE'
          const fs = require('fs')

          function readJson(p) { return JSON.parse(fs.readFileSync(p, 'utf8')) }
          function writeJson(p, v) { fs.writeFileSync(p, `${JSON.stringify(v, null, 2)}\n`) }

          const appJson = readJson('app.json')
          const plugins = appJson.expo?.plugins
          if (Array.isArray(plugins)) {
            const filtered = plugins.filter(p => p !== 'expo-web-browser' && !(Array.isArray(p) && p[0] === 'expo-web-browser'))
            if (filtered.length !== plugins.length) {
              appJson.expo.plugins = filtered
              writeJson('app.json', appJson)
            }
          }

          if (!appJson.expo?.version) throw new Error('app.json is missing expo.version')
          NODE

      - name: Install JavaScript dependencies
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"

          corepack enable
          corepack prepare yarn@1.22.22 --activate
          yarn --version

          yarn install --ignore-engines --ignore-scripts --network-timeout 600000

          ./node_modules/.bin/patch-package --patch-dir packages/android/patches || echo "::warning::Some Android patch-package patches did not apply; continuing."

      - name: Build Rust native library
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE/android/rust"

          if [ ! -f build-android-linux.sh ]; then
            echo "No Rust build script found, skipping."
            exit 0
          fi

          export ANDROID_HOME="${ANDROID_HOME:-$ANDROID_SDK_ROOT}"
          export ANDROID_NDK_HOME="$ANDROID_HOME/ndk/27.1.12297006"
          export ANDROID_NDK_ROOT="$ANDROID_NDK_HOME"

          echo "y" | bash build-android-linux.sh

      - name: Clear Metro cache
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"
          rm -rf /tmp/metro-* /tmp/haste-map-* node_modules/.cache 2>/dev/null || true
          echo "Metro/Haste cache cleared before bundling"

      - name: Build Release APK
        env:
          NODE_ENV: production
          MYAPP_RELEASE_STORE_FILE: ${{ env.KEYSTORE_PATH }}
          MYAPP_RELEASE_STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          MYAPP_RELEASE_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          MYAPP_RELEASE_KEY_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE/android"

          chmod +x ./gradlew

          if [ ! -f "$MYAPP_RELEASE_STORE_FILE" ]; then
            echo "::error::Keystore not found: $MYAPP_RELEASE_STORE_FILE"
            exit 1
          fi

          ./gradlew assembleRelease \
            --no-daemon \
            --stacktrace \
            -PMYAPP_RELEASE_STORE_FILE="$MYAPP_RELEASE_STORE_FILE" \
            -PMYAPP_RELEASE_STORE_PASSWORD="$MYAPP_RELEASE_STORE_PASSWORD" \
            -PMYAPP_RELEASE_KEY_ALIAS="$MYAPP_RELEASE_KEY_ALIAS" \
            -PMYAPP_RELEASE_KEY_PASSWORD="$MYAPP_RELEASE_KEY_PASSWORD" \
            -Dorg.gradle.jvmargs="-Xmx4g -XX:+HeapDumpOnOutOfMemoryError" \
            -Dorg.gradle.parallel=true

      - name: Package APK
        env:
          ASSET_NAME: ${{ steps.version.outputs.asset_name }}
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"

          apk_dir="android/app/build/outputs/apk/release"
          apk_path="$(ls "$apk_dir"/app-*-release.apk 2>/dev/null | head -1 || ls "$apk_dir"/app-release.apk 2>/dev/null | head -1)"

          if [ -z "$apk_path" ] || [ ! -f "$apk_path" ]; then
            echo "::error::No release APK found in $apk_dir/"
            ls -la "$apk_dir/" 2>/dev/null || true
            exit 1
          fi

          target_apk="$GITHUB_WORKSPACE/$ASSET_NAME"
          cp "$apk_path" "$target_apk"
          echo "APK_PATH=$target_apk" >> "$GITHUB_ENV"

      - name: Build and package clone APK
        env:
          NODE_ENV: production
          MYAPP_RELEASE_STORE_FILE: ${{ env.KEYSTORE_PATH }}
          MYAPP_RELEASE_STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          MYAPP_RELEASE_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          MYAPP_RELEASE_KEY_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          CLONE_ASSET_NAME: ${{ steps.version.outputs.clone_asset_name }}
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE/android"

          sed -i -E 's/applicationId "[^"]*"/applicationId "com.ldp924.bangumi"/' app/build.gradle

          rm -rf app/build/outputs/apk/release/

          ./gradlew assembleRelease \
            --no-daemon \
            --stacktrace \
            -PMYAPP_RELEASE_STORE_FILE="$MYAPP_RELEASE_STORE_FILE" \
            -PMYAPP_RELEASE_STORE_PASSWORD="$MYAPP_RELEASE_STORE_PASSWORD" \
            -PMYAPP_RELEASE_KEY_ALIAS="$MYAPP_RELEASE_KEY_ALIAS" \
            -PMYAPP_RELEASE_KEY_PASSWORD="$MYAPP_RELEASE_KEY_PASSWORD" \
            -Dorg.gradle.jvmargs="-Xmx4g -XX:+HeapDumpOnOutOfMemoryError" \
            -Dorg.gradle.parallel=true

          cd "$GITHUB_WORKSPACE"
          apk_dir="android/app/build/outputs/apk/release"
          apk_path="$(ls "$apk_dir"/app-*-release.apk 2>/dev/null | head -1 || ls "$apk_dir"/app-release.apk 2>/dev/null | head -1)"

          if [ -z "$apk_path" ] || [ ! -f "$apk_path" ]; then
            echo "::error::No clone APK found"
            exit 1
          fi

          target_apk="$GITHUB_WORKSPACE/$CLONE_ASSET_NAME"
          cp "$apk_path" "$target_apk"
          echo "CLONE_APK_PATH=$target_apk" >> "$GITHUB_ENV"

      - name: Validate APK
        run: |
          set -euo pipefail

          for apk in "$APK_PATH" "$CLONE_APK_PATH"; do
            if [ ! -f "$apk" ]; then
              echo "::error::APK not found: $apk"
              exit 1
            fi

            apk_size="$(stat -c%s "$apk")"
            echo "$(basename "$apk") size: $apk_size bytes"

            if [ "$apk_size" -lt 10485760 ]; then
              echo "::warning::APK size is less than 10MB: $apk_size bytes ($apk)"
            fi
          done

          APKSIGNER="$(find "${ANDROID_HOME:-$ANDROID_SDK_ROOT}" -name apksigner 2>/dev/null | head -1)"
          if [ -n "$APKSIGNER" ] && [ -x "$APKSIGNER" ]; then
            "$APKSIGNER" verify --verbose "$APK_PATH"
            "$APKSIGNER" verify --verbose "$CLONE_APK_PATH"
            echo "APK signatures verified"
          else
            echo "::warning::apksigner not found, skipping verification"
          fi

      - name: Compose release notes (上游提交日志)
        id: notes
        env:
          INPUT_NOTES: ${{ inputs.release_notes }}
        run: |
          set -euo pipefail

          if [ -n "$INPUT_NOTES" ]; then
            # sync 工作流传入: 上游最新一次 push 会话的提交 (首选)
            printf '%s\n' "$INPUT_NOTES" > /tmp/release_notes.md
            echo "notes_source=sync-dispatch" >> "$GITHUB_OUTPUT"
          else
            # 手动触发: 上游作者(czy0729)最新提交 1h 聚簇取最新推送会话
            # (fork 同步历史结构不稳定, 按作者过滤天然排除 fork 自有 commit)
            git log --author='czy0729' -n 60 --no-merges \
              --pretty=format:'%ci|%H|%s' | python3 .github/scripts/cluster-notes.py \
              > /tmp/release_notes.md
          fi

          echo "=== 发行版日志 ==="
          cat /tmp/release_notes.md

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BUILD_TAG: ${{ steps.version.outputs.build_tag }}
        run: |
          set -euo pipefail

          if gh release view "$BUILD_TAG" >/dev/null 2>&1; then
            gh release upload "$BUILD_TAG" "$APK_PATH" "$CLONE_APK_PATH" --clobber
          else
            gh release create "$BUILD_TAG" "$APK_PATH" "$CLONE_APK_PATH" \
              --title "Bangumi $BUILD_TAG" \
              --notes-file /tmp/release_notes.md
          fi

          echo "Release: https://github.com/${{ github.repository }}/releases/tag/$BUILD_TAG"
